{
  "name": "handsfree",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && vite build --config vite.lib.config.ts && vite build --config vite.lib.esm.config.ts",
    "preview": "vite preview",
    "traces": "node scripts/generate-traces.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
